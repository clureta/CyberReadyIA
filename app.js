(()=>{const D=window.CYBERREADY_DEMO;let selected=D.scenarios[0],completedScenario=null;const ratings={};const $=(s,c=document)=>c.querySelector(s),$$=(s,c=document)=>[...c.querySelectorAll(s)];const titles={home:'¿Estamos realmente preparados?',twin:'Decision Twin conceptual',simulate:'Simular antes de la crisis',results:'De datos a decisiones',chaos:'Romper para aprender',value:'Hipótesis de valor',feedback:'Validar antes de construir'};function go(v){$$('.view').forEach(x=>x.classList.toggle('active',x.id===`view-${v}`));$$('#nav button').forEach(b=>b.classList.toggle('active',b.dataset.view===v));$('#pageTitle').textContent=titles[v]||'CyberReady AI';window.scrollTo({top:0,behavior:'smooth'})}$$('[data-view]').forEach(b=>b.addEventListener('click',()=>go(b.dataset.view)));$$('[data-go]').forEach(b=>b.addEventListener('click',()=>go(b.dataset.go)));function renderScenarios(){$('#scenarioCards').innerHTML=D.scenarios.map(s=>`<article class="scenario-card ${s.id===selected.id?'selected':''}" data-scenario="${s.id}"><div class="scenario-icon">${s.icon}</div><h3>${s.title}</h3><p>${s.subtitle}</p><small>Score base simulado: ${s.baseScore}/100</small></article>`).join('');$$('.scenario-card').forEach(c=>c.addEventListener('click',()=>selectScenario(c.dataset.scenario)))}function selectScenario(id){selected=D.scenarios.find(s=>s.id===id);renderScenarios();$('#simulationPanel').classList.remove('hidden');$('#incidentTitle').textContent=selected.title;const f=selected.event;$('#incidentFacts').innerHTML=[['Hora',f.time],['Momento',f.day],['Activo',f.asset],['Registros',f.records],['Severidad',f.severity]].map(x=>`<div class="fact"><span>${x[0]}</span><b>${x[1]}</b></div>`).join('');$('#agentSteps').innerHTML=selected.agents.map((a,i)=>`<div class="agent-step" data-agent="${i}"><span class="agent-state">${i+1}</span><div><b>${a[0]}</b><small>${a[1]}</small></div></div>`).join('');$('#runAgents').disabled=false;$('#runAgents').textContent='Iniciar simulación de agentes'}$('#cancelSim').addEventListener('click',()=>$('#simulationPanel').classList.add('hidden'));function runAgents(){const btn=$('#runAgents');btn.disabled=true;btn.textContent='Simulando…';const steps=$$('.agent-step');let i=0;function next(){if(i>0){steps[i-1].classList.remove('running');steps[i-1].classList.add('done');steps[i-1].querySelector('.agent-state').textContent='✓'}if(i<steps.length){steps[i].classList.add('running');i++;setTimeout(next,620)}else{completedScenario=selected;setTimeout(()=>{renderResults();renderChaos();go('results');toast('Simulación completada')},380)}}next()}$('#runAgents').addEventListener('click',runAgents);function renderResults(){if(!completedScenario)return;$('#emptyResults').classList.add('hidden');$('#resultsContent').classList.remove('hidden');$('#resultScenario').textContent=completedScenario.title;$('#resultScore').textContent=completedScenario.baseScore;$('#findings').innerHTML=completedScenario.findings.map(f=>`<article class="finding"><div class="finding-head"><span class="sev ${f.sev}">${f.sev==='critical'?'CRÍTICO':f.sev==='high'?'ALTO':'MEDIO'}</span><b>${f.title}</b><span>＋</span></div><div class="finding-body"><p><strong>Impacto:</strong> ${f.impact}</p><p><strong>Evidencia simulada:</strong> ${f.evidence}</p><p><strong>Recomendación:</strong> ${f.recommendation}</p><p><strong>HITL:</strong> la decisión final requeriría validación humana.</p></div></article>`).join('');$$('.finding-head').forEach(h=>h.addEventListener('click',()=>h.parentElement.classList.toggle('open')))}function renderChaos(){const base=(completedScenario||selected).baseScore;$('#chaosBase').textContent=base;$('#chaosScore').textContent=base;$('#chaosOptions').innerHTML=D.chaos.map(c=>`<label class="chaos-option"><input type="checkbox" value="${c.id}"><div><b>${c.label}</b><small>${c.reason}</small></div><span class="penalty">−${c.penalty}</span></label>`).join('');$$('#chaosOptions input').forEach(i=>i.addEventListener('change',updateChaos));updateChaos()}function updateChaos(){const base=(completedScenario||selected).baseScore,active=$$('#chaosOptions input:checked').map(x=>D.chaos.find(c=>c.id===x.value)),penalty=active.reduce((a,c)=>a+c.penalty,0),score=Math.max(15,base-penalty);$('#chaosScore').textContent=score;const d=$('#chaosDelta');d.textContent=active.length?`−${base-score} puntos por ${active.length} falla${active.length>1?'s':''}`:'Sin fallas adicionales';d.className=`delta ${active.length?'bad':'neutral'}`;$('#chaosReasons').innerHTML=active.map(c=>`<div class="reason">${c.label}: ${c.reason}</div>`).join('')}$('#clearChaos').addEventListener('click',()=>{$$('#chaosOptions input').forEach(i=>i.checked=false);updateChaos()});const qs=[['¿El problema refleja una preocupación real?','1 = baja relevancia · 5 = crítica'],['¿El Decision Twin aporta algo distinto a un GRC o tabletop?','Evalúa diferenciación percibida'],['¿Los hallazgos mostrados serían accionables?','Valida valor operacional'],['¿Qué tan útil sería simular fallas de proveedores/personas?','Valida Regulatory Chaos'],['¿Pagaría por una solución así si demostrara ROI?','Valida intención comercial'],['¿Un piloto de 4–6 semanas sería razonable?','Valida formato de adopción']];$('#questions').innerHTML=qs.map((q,i)=>`<article class="question"><b>${q[0]}</b><span>${q[1]}</span><div class="rating" data-q="${i}">${[1,2,3,4,5].map(n=>`<button>${n}</button>`).join('')}</div></article>`).join('');$$('.rating button').forEach(b=>b.addEventListener('click',()=>{const r=b.parentElement;$$('button',r).forEach(x=>x.classList.remove('selected'));b.classList.add('selected');ratings[r.dataset.q]=Number(b.textContent);localStorage.setItem('cyberready-ratings',JSON.stringify(ratings))}));$('#notes').value=localStorage.getItem('cyberready-notes')||'';$('#notes').addEventListener('input',e=>localStorage.setItem('cyberready-notes',e.target.value));function feedbackText(){return`CYBERREADY AI — ENTREVISTA DE VALIDACIÓN\nFecha: ${new Date().toLocaleString()}\n\nPuntajes (1-5):\n${qs.map((q,i)=>`- ${q[0]}: ${ratings[i]||'sin respuesta'}`).join('\n')}\n\nNotas:\n${$('#notes').value||'(sin notas)'}\n\nDemo de ideación. No representa cumplimiento ni resultados reales.`}$('#copyNotes').addEventListener('click',async()=>{try{await navigator.clipboard.writeText(feedbackText());toast('Resumen copiado')}catch{fallbackCopy(feedbackText())}});$('#downloadNotes').addEventListener('click',()=>{const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([feedbackText()],{type:'text/plain'}));a.download='cyberready-validacion.txt';a.click();URL.revokeObjectURL(a.href)});function fallbackCopy(t){const ta=document.createElement('textarea');ta.value=t;document.body.appendChild(ta);ta.select();document.execCommand('copy');ta.remove();toast('Resumen copiado')}function toast(t){const el=$('#toast');el.textContent=t;el.classList.add('show');setTimeout(()=>el.classList.remove('show'),1800)}$('#resetBtn').addEventListener('click',()=>{completedScenario=null;selected=D.scenarios[0];localStorage.removeItem('cyberready-ratings');localStorage.removeItem('cyberready-notes');location.reload()});renderScenarios();selectScenario(selected.id);renderChaos()})();
